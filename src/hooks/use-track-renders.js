import { useEffect, useState } from 'react'

const useTrackRenders = (state, component) => {
    const [mounted, setMounted] = useState(false)
    
    useEffect(() => {
        if (mounted && state.length >= 1) console.log(`${component} rendered because state[0] changed to:`, state[0]);
    }, [state[0], component]);
    useEffect(() => {
        if (mounted && state.length >= 2) console.log(`${component} rendered because state[1] changed to:`, state[1]);
    }, [state[1], component]);
    useEffect(() => {
        if (mounted && state.length >= 3) console.log(`${component} rendered because state[2] changed to:`, state[2]);
    }, [state[2], component]);
    useEffect(() => {
        if (mounted && state.length >= 4) console.log(`${component} rendered because state[3] changed to:`, state[3]);
    }, [state[3], component]);
    useEffect(() => {
        if (mounted && state.length >= 5) console.log(`${component} rendered because state[4] changed to:`, state[4]);
    }, [state[4], component]);
    useEffect(() => {
        if (mounted && state.length >= 6) console.log(`${component} rendered because state[5] changed to:`, state[5]);
    }, [state[5], component]);
    useEffect(() => {
        if (mounted && state.length >= 7) console.log(`${component} rendered because state[6] changed to:`, state[6]);
    }, [state[6], component]);
    useEffect(() => {
        if (mounted && state.length >= 8) console.log(`${component} rendered because state[7] changed to:`, state[7]);
    }, [state[7], component]);
    useEffect(() => {
        if (mounted && state.length >= 9) console.log(`${component} rendered because state[8] changed to:`, state[8]);
    }, [state[8], component]);
    useEffect(() => {
        if (mounted && state.length >= 10) console.log(`${component} rendered because state[9] changed to:`, state[9]);
    }, [state[9], component]);
    useEffect(() => {
        if (mounted && state.length >= 11) console.log(`${component} rendered because state[10] changed to:`, state[10]);
    }, [state[10], component]);
    useEffect(() => {
        if (mounted && state.length >= 12) console.log(`${component} rendered because state[11] changed to:`, state[11]);
    }, [state[11], component]);
    useEffect(() => {
        if (mounted && state.length >= 13) console.log(`${component} rendered because state[12] changed to:`, state[12]);
    }, [state[12], component]);
    useEffect(() => {
        if (mounted && state.length >= 14) console.log(`${component} rendered because state[13] changed to:`, state[13]);
    }, [state[13], component]);
    useEffect(() => {
        if (mounted && state.length >= 15) console.log(`${component} rendered because state[14] changed to:`, state[14]);
    }, [state[14], component]);
    useEffect(() => {
        if (mounted && state.length >= 16) console.log(`${component} rendered because state[15] changed to:`, state[15]);
    }, [state[15], component]);
    useEffect(() => {
        if (mounted && state.length >= 17) console.log(`${component} rendered because state[16] changed to:`, state[16]);
    }, [state[16], component]);
    useEffect(() => {
        if (mounted && state.length >= 18) console.log(`${component} rendered because state[17] changed to:`, state[17]);
    }, [state[17], component]);
    useEffect(() => {
        if (mounted && state.length >= 19) console.log(`${component} rendered because state[18] changed to:`, state[18]);
    }, [state[18], component]);
    useEffect(() => {
        if (mounted && state.length >= 20) console.log(`${component} rendered because state[19] changed to:`, state[19]);
    }, [state[19], component]);
    useEffect(() => {
        if (mounted && state.length >= 21) console.log(`${component} rendered because state[20] changed to:`, state[20]);
    }, [state[20], component]);
    useEffect(() => {
        if (mounted && state.length >= 22) console.log(`${component} rendered because state[21] changed to:`, state[21]);
    }, [state[21], component]);
    useEffect(() => {
        if (mounted && state.length >= 23) console.log(`${component} rendered because state[22] changed to:`, state[22]);
    }, [state[22], component]);
    useEffect(() => {
        if (mounted && state.length >= 24) console.log(`${component} rendered because state[23] changed to:`, state[23]);
    }, [state[23], component]);
    useEffect(() => {
        if (mounted && state.length >= 25) console.log(`${component} rendered because state[24] changed to:`, state[24]);
    }, [state[24], component]);
    useEffect(() => {
        if (mounted && state.length >= 26) console.log(`${component} rendered because state[25] changed to:`, state[25]);
    }, [state[25], component]);
    useEffect(() => {
        if (mounted && state.length >= 27) console.log(`${component} rendered because state[26] changed to:`, state[26]);
    }, [state[26], component]);
    useEffect(() => {
        if (mounted && state.length >= 28) console.log(`${component} rendered because state[27] changed to:`, state[27]);
    }, [state[27], component]);
    useEffect(() => {
        if (mounted && state.length >= 29) console.log(`${component} rendered because state[28] changed to:`, state[28]);
    }, [state[28], component]);
    useEffect(() => {
        if (mounted && state.length >= 30) console.log(`${component} rendered because state[29] changed to:`, state[29]);
    }, [state[29], component]);
    useEffect(() => {
        if (mounted && state.length >= 31) console.log(`${component} rendered because state[30] changed to:`, state[30]);
    }, [state[30], component]);
    useEffect(() => {
        if (mounted && state.length >= 32) console.log(`${component} rendered because state[31] changed to:`, state[31]);
    }, [state[31], component]);
    useEffect(() => {
        if (mounted && state.length >= 33) console.log(`${component} rendered because state[32] changed to:`, state[32]);
    }, [state[32], component]);
    useEffect(() => {
        if (mounted && state.length >= 34) console.log(`${component} rendered because state[33] changed to:`, state[33]);
    }, [state[33], component]);
    useEffect(() => {
        if (mounted && state.length >= 35) console.log(`${component} rendered because state[34] changed to:`, state[34]);
    }, [state[34], component]);
    useEffect(() => {
        if (mounted && state.length >= 36) console.log(`${component} rendered because state[35] changed to:`, state[35]);
    }, [state[35], component]);
    useEffect(() => {
        if (mounted && state.length >= 37) console.log(`${component} rendered because state[36] changed to:`, state[36]);
    }, [state[36], component]);
    useEffect(() => {
        if (mounted && state.length >= 38) console.log(`${component} rendered because state[37] changed to:`, state[37]);
    }, [state[37], component]);
    useEffect(() => {
        if (mounted && state.length >= 39) console.log(`${component} rendered because state[38] changed to:`, state[38]);
    }, [state[38], component]);
    useEffect(() => {
        if (mounted && state.length >= 40) console.log(`${component} rendered because state[39] changed to:`, state[39]);
    }, [state[39], component]);

    useEffect(() => {
        setMounted(true);
    },[])

    return null;
}

export default useTrackRenders